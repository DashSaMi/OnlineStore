{
  "name": "onlinestore",
  "private": true,
  "scripts": {
    "build:shop": "cd OnlineStore/shop && npm install && next build",
    "build:dashboard": "cd OnlineStore/dashboard && npm install && next build"
  }
}
